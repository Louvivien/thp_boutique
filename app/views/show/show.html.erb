<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
    </head>
	
    <body>
	    <img src="<%= @item.image_url %>">
	    </br>
	    <%= @item.title %>
	    </br>
	    <%=  @item.description %>
	    </br>
	    <%= @item.price %> $
    </body>
</html>
