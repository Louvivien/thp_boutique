<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>E-boutique(MVP)</title>
    </head>

    <body>
		<h1>Photo de chat(MVP)</h1>
		<%= @item.each do |item1| %>	
			<%= link_to "Show", profile_path(@item1) do %>
				    <img src="<%= @item1.image_url %>">
		<% end %>
    </body>
</html>
