<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>E-boutique(MVP)</title>
    </head>

    <body>
		<h1>Photo de chat(MVP)</h1>
		<% @array.each do |item1| %>
		  <div class="card">
		  	<%= item1.title %>
		  	<div class="img_card">
			  <%= link_to kitty_path(item1) do %>
			  	<img src="<%= item1.image_url %>" width="100%">
			  <% end %>
		    </div>
		  </div>
		<% end %>
    </body>
</html>
